[build-system]
build-backend = "setuptools.build_meta"

requires = [ "setuptools>=80", "setuptools-scm[toml]>=6.2" ]

[project]
name = "fiab-plugin-ecmwf"
description = "Weather Forecast in a Box, ECMWF plugin"
readme = "README.md"
license = "Apache-2.0"
license-files = [ "LICENSE" ]
authors = [
  { name = "European Centre for Medium-Range Weather Forecasts (ECMWF)", email = "software.support@ecmwf.int" },
]
requires-python = ">=3.11"

dynamic = [ "version" ]

dependencies = [
    "fiab-core>=5,<6", # auto-updateable -- do not remove this comment
    "earthkit-workflows>=0.15.6",
    "earthkit-workflows-anemoi>=0.7.2",
    "pproc-core<0.4.0",
    "pymetkit>=1.21.0.28",
]

optional-dependencies.runtime = [
    "earthkit-data>=0.18.2,<1",
    "earthkit>=0.13.2",
    "earthkit-plots>=0.3.5,<1.0",
    "earthkit-workflows-anemoi[runtime-inference]",
]

[dependency-groups]
dev = [
  "ty==0.0.2",
  "pytest_lazy_fixtures",
]

[tool.setuptools_scm]
version_file = "src/fiab_plugin_ecmwf/_version.py"
write_to_template = '''# Do not change! Do not track in version control!
__version__ = "{version}"
'''
local_scheme = "no-local-version"
root = ".."
fallback_version = "5.0.0" # auto-updateable -- do not remove this comment
tag_regex = '^[vd](?P<version>\d+(?:\.\d+){0,2}[^\+]*)(?:\+.*)?$'

[tool.ruff]
line-length = 140

lint.ignore = [ "E731", "ANN401" ]
lint.select = ["I", "ANN"]

[tool.pytest.ini_options]
log_cli = true
